import React from "react";
import HeroCarousel from "@/components/HeroCarousel";

const page = () => {
  return (
    <main className="min-h-screen">
      <HeroCarousel />
    </main>
  );
};

export default page;
